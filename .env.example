# Мок-режим: приложение полностью работает без бэкенда.
EXPO_PUBLIC_MOCK_AI=1

# URL функций Supabase. Когда задан — используется вместо моков.
EXPO_PUBLIC_API_URL=

# Только на сервере (supabase secrets set), не в клиенте:
#   ANTHROPIC_API_KEY=
#   ANTHROPIC_MODEL=claude-sonnet-4-6
#   ALLOWED_ORIGIN=
